{
  "name": "wtracks-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for W.Tracks audio storage with R2",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}